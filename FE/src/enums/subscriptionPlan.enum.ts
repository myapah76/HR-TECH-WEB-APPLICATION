export enum SubscriptionType {
  CANDIDATE = 'CANDIDATE',
  COMPANY = 'COMPANY',
}

export enum ResetType {
  DAILY = 'DAILY',
  WEEKLY = 'WEEKLY',
}

export enum SubscriptionStatus {
  ACTIVE = 'ACTIVE',
  EXPIRED = 'EXPIRED',
  INACTIVE = 'INACTIVE',
  CANCELLED = 'CANCELLED',
  PENDING = 'PENDING',
}
